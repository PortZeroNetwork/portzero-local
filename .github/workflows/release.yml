name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  version:
    name: Determine Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Generate version
        id: version
        run: |
          LATEST_TAG=$(git tag -l "v[0-9]*.[0-9]*.[0-9]*" | sort -V | tail -1)
          LATEST_TAG="${LATEST_TAG:-v0.0.0}"

          MAJOR=$(echo "$LATEST_TAG" | sed 's/^v//' | cut -d. -f1)
          MINOR=$(echo "$LATEST_TAG" | cut -d. -f2)
          PATCH=$(echo "$LATEST_TAG" | cut -d. -f3)

          COMMIT_MSG=$(git log -1 --pretty=%B)
          if echo "$COMMIT_MSG" | grep -qE "BREAKING CHANGE|\[major\]"; then
            MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0
          elif echo "$COMMIT_MSG" | grep -qE "^feat(\([^)]+\))?:|\[minor\]"; then
            MINOR=$((MINOR + 1)); PATCH=0
          else
            PATCH=$((PATCH + 1))
          fi

          VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.target }}
    needs: version
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            bins: portzero
            archive_ext: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            bins: portzero
            cross: true
            archive_ext: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            bins: portzero
            archive_ext: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            bins: portzero
            archive_ext: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            bins: portzero
            archive_ext: zip

    steps:
      - uses: actions/checkout@v5

      - name: Set CLI package version
        run: scripts/set-cli-version.sh "${{ needs.version.outputs.version }}"
        shell: bash

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          save-if: ${{ matrix.os != 'windows-latest' }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build
        env:
          PKG_CONFIG_SYSROOT_DIR: ${{ matrix.cross && '/' || '' }}
        run: |
          build_cmd="cargo"
          if [ "${{ matrix.cross }}" = "true" ]; then
            build_cmd="cross"
          fi

          IFS=',' read -ra BINS <<< "${{ matrix.bins }}"
          bin_flags=""
          for bin in "${BINS[@]}"; do
            bin_flags="$bin_flags --bin $bin"
          done

          $build_cmd build --release --target ${{ matrix.target }} $bin_flags
        shell: bash

      - name: Package (Unix)
        if: matrix.archive_ext == 'tar.gz'
        run: |
          staging="port-zero-${{ matrix.target }}"
          mkdir -p "$staging"

          IFS=',' read -ra BINS <<< "${{ matrix.bins }}"
          for bin in "${BINS[@]}"; do
            cp "target/${{ matrix.target }}/release/$bin" "$staging/"
          done

          tar czf "$staging.tar.gz" "$staging"

      - name: Package (Windows)
        if: matrix.archive_ext == 'zip'
        shell: pwsh
        run: |
          $staging = "port-zero-${{ matrix.target }}"
          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          foreach ($bin in "${{ matrix.bins }}".Split(",")) {
            Copy-Item "target\${{ matrix.target }}\release\$bin.exe" "$staging\"
          }

          Invoke-WebRequest `
            -Uri "https://www.wintun.net/builds/wintun-0.14.1.zip" `
            -OutFile "wintun.zip"
          Expand-Archive -Path "wintun.zip" -DestinationPath "wintun" -Force
          Copy-Item "wintun\wintun\bin\amd64\wintun.dll" "$staging\"

          Compress-Archive -Path "$staging" -DestinationPath "$staging.zip"

      - name: Compute SHA256 (macOS)
        if: contains(matrix.target, 'apple-darwin')
        run: |
          shasum -a 256 "port-zero-${{ matrix.target }}.tar.gz" | awk '{print $1}' \
            > "port-zero-${{ matrix.target }}.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: port-zero-${{ matrix.target }}
          path: port-zero-${{ matrix.target }}.${{ matrix.archive_ext }}

      - name: Upload SHA256 (macOS)
        if: contains(matrix.target, 'apple-darwin')
        uses: actions/upload-artifact@v5
        with:
          name: sha256-${{ matrix.target }}
          path: port-zero-${{ matrix.target }}.sha256

  package-linux:
    name: Build Linux Packages
    needs: [version, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set CLI package version
        run: scripts/set-cli-version.sh "${{ needs.version.outputs.version }}"
        shell: bash

      - name: Download x86_64 Linux artifact
        uses: actions/download-artifact@v5
        with:
          name: port-zero-x86_64-unknown-linux-gnu

      - name: Extract binary into target/release
        run: |
          tar xzf port-zero-x86_64-unknown-linux-gnu.tar.gz
          mkdir -p target/release
          cp port-zero-x86_64-unknown-linux-gnu/portzero target/release/portzero
          chmod +x target/release/portzero

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install packaging tools
        run: cargo install cargo-deb cargo-generate-rpm --locked

      - name: Build .deb package
        run: cargo deb -p port-zero-cli --no-build

      - name: Build .rpm package
        run: cargo generate-rpm
        working-directory: client/crates/cli

      - name: Upload .deb
        uses: actions/upload-artifact@v5
        with:
          name: portzero-deb
          path: target/debian/*.deb

      - name: Upload .rpm
        uses: actions/upload-artifact@v5
        with:
          name: portzero-rpm
          path: target/generate-rpm/*.rpm

  package-windows:
    name: Build Windows MSI
    needs: [version, build]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Download x86_64 Windows artifact
        uses: actions/download-artifact@v5
        with:
          name: port-zero-x86_64-pc-windows-msvc

      - name: Extract Windows payload
        shell: pwsh
        run: |
          Expand-Archive `
            -Path "port-zero-x86_64-pc-windows-msvc.zip" `
            -DestinationPath "." `
            -Force

      - name: Install WiX Toolset
        shell: pwsh
        run: choco install wixtoolset --no-progress -y

      - name: Build MSI
        shell: pwsh
        run: |
          $sourceDir = (Resolve-Path ".\port-zero-x86_64-pc-windows-msvc").Path
          $wixRoot = "${env:ProgramFiles(x86)}\WiX Toolset*"
          $candle = Get-ChildItem $wixRoot -Recurse -Filter candle.exe |
            Select-Object -First 1
          $light = Get-ChildItem $wixRoot -Recurse -Filter light.exe |
            Select-Object -First 1

          if (-not $candle -or -not $light) {
            throw "WiX Toolset candle.exe/light.exe not found"
          }

          & $candle.FullName `
            "-dProductVersion=${{ needs.version.outputs.version }}" `
            "-dSourceDir=$sourceDir" `
            -out "portzero.wixobj" `
            "packaging\windows\portzero.wxs"

          & $light.FullName `
            -out "portzero-${{ needs.version.outputs.version }}-x86_64.msi" `
            "portzero.wixobj"

      - name: Upload MSI
        uses: actions/upload-artifact@v5
        with:
          name: portzero-msi
          path: "*.msi"

  release:
    name: Create Release
    needs: [version, build, package-linux, package-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts
          pattern: port-zero-*
          merge-multiple: true

      - name: Download OS packages
        uses: actions/download-artifact@v5
        with:
          path: artifacts
          pattern: portzero-*
          merge-multiple: true

      - name: Generate version manifest
        run: |
          echo "{\"version\":\"${{ needs.version.outputs.version }}\",\"commit\":\"${GITHUB_SHA}\"}" \
            > artifacts/version.json

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.version.outputs.tag }}
          generate_release_notes: true
          files: |
            artifacts/*.tar.gz
            artifacts/*.zip
            artifacts/*.deb
            artifacts/*.rpm
            artifacts/*.msi
            artifacts/version.json
            scripts/install.sh

  check-homebrew-token:
    name: Check Homebrew Token
    needs: [release]
    runs-on: ubuntu-latest
    outputs:
      has-token: ${{ steps.check.outputs.has-token }}
    steps:
      - name: Check token presence
        id: check
        run: echo "has-token=${{ secrets.HOMEBREW_TAP_TOKEN != '' }}" >> "$GITHUB_OUTPUT"

  update-homebrew:
    name: Update Homebrew Formula
    needs: [version, release, check-homebrew-token]
    runs-on: ubuntu-latest
    if: ${{ needs.check-homebrew-token.outputs.has-token == 'true' }}
    steps:
      - name: Download macOS SHA256 files
        uses: actions/download-artifact@v5
        with:
          pattern: sha256-*apple-darwin
          merge-multiple: true

      - name: Checkout tap repository
        uses: actions/checkout@v5
        with:
          repository: PortZeroNetwork/homebrew-portzero
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: tap

      - name: Update formula
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          AARCH64_SHA256=$(cat port-zero-aarch64-apple-darwin.sha256)
          X86_64_SHA256=$(cat port-zero-x86_64-apple-darwin.sha256)

          sed -i "s/version \"[^\"]*\"/version \"${VERSION}\"/" tap/Formula/portzero.rb
          sed -i "s/sha256 \"[^\"]*\" # arm64/sha256 \"${AARCH64_SHA256}\" # arm64/" tap/Formula/portzero.rb
          sed -i "s/sha256 \"[^\"]*\" # x86_64/sha256 \"${X86_64_SHA256}\" # x86_64/" tap/Formula/portzero.rb

      - name: Commit and push
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/portzero.rb
          git diff --staged --quiet || git commit -m "port-zero v${VERSION}"
          git push
