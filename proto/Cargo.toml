[package]
name = "port-zero-proto"
version = "0.1.0"
edition = "2021"
license = "PolyForm-Shield-1.0.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
