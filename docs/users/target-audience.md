# Target audience: users

People who **use** Port Zero Local as a product: developers and teams running
local processes or Docker containers who need stable hostnames without fixed
ports, plus optional cloud tunnels.

They are not necessarily contributors to this repository. Prefer product
language (Local tunnel, Cloud tunnel, `PZ_TUNNEL`) over implementation detail
unless it explains a user-visible failure.
